import React from 'react';

//es una pagina contenedora, todavia sin logica.
// Este componente representa la PÁGINA de clientes.
// Normalmente se conecta a una ruta como: /clientes
const ClientesPage = () => {
    return (
        <div>
            {/* Título principal de la página */}
            <h2>Clientes</h2>

            {/* Texto informativo mientras se implementa la funcionalidad */}
            <p>Página de clientes (pendiente de implementar)</p>
        </div>
    );
};

export default ClientesPage;
